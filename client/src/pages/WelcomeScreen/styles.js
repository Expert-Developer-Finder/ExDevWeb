import { makeStyles } from '@material-ui/core/styles';

export default makeStyles((theme) => ({
    container: {
        marginTop: 35,
    },
    subtitle: {
        fontSize: "larger",
        marginLeft:10

    },
    buttons: {
        marginTop: 15,

    },
    button: {
        marginLeft:10,
    }
}));